import 'package:flutter/material.dart';

class StreetViewLogic extends StatelessWidget {
  const StreetViewLogic({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Street View Logic'),
      ),
    );
  }
}