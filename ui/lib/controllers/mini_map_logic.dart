import 'package:flutter/material.dart';

class MiniMapLogic extends StatelessWidget {
  const MiniMapLogic({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Mini Map Logic'),
      ),
    );
  }

}